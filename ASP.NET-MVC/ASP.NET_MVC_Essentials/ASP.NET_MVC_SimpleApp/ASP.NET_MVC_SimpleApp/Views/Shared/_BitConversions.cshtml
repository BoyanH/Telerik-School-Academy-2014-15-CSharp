@model IList<KeyValuePair<string, double>>

<table class="table table-striped table-condensed">
    @foreach (KeyValuePair<string, double> pair in Model)
    {
        <tr>
            <td>@pair.Key</td>
            <td>@pair.Value</td>
        </tr>
    }
</table>